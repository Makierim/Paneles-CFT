@model IEnumerable<Paneles_CFT.Models.ProyectosComponentesMap>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaInstalacionComponente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoUsoComponente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodigoComponentePsCsNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProyectoPsCsNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaInstalacionComponente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoUsoComponente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoComponentePsCsNavigation.CodigoComponente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProyectoPsCsNavigation.IdProyecto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdProyectosComponentes">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdProyectosComponentes">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdProyectosComponentes">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
